services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfile.app.dev
    ports:
      - "5100:5100"
    volumes:
      - ./app:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
      - TURBOPACK_WATCH_OPTIONS_POLL_INTERVAL=1000